fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic[email]==2.10.4
pydantic-settings==2.7.0

pyjwt==2.10.1
bcrypt==4.2.1

sqlalchemy==2.0.36
psycopg[binary]==3.2.3
alembic==1.14.0
# SQLAlchemy type and operator support for the pgvector extension. The server
# side is provided by the pgvector/pgvector image (see docker-compose.yml);
# this package only teaches SQLAlchemy how to bind and render the type.
pgvector==0.3.6

sentence-transformers==3.3.1
pymupdf==1.25.1

# chromadb used to cap tokenizers at <=0.20.3, which in turn pinned
# transformers to 4.46.x. Removing Chroma removed that constraint, so these
# are now free to move with sentence-transformers. They stay pinned because a
# tokenizer change alters how text is counted into chunks, and a silent chunk
# boundary shift would invalidate every benchmark number in the repo.
transformers==4.47.1
tokenizers==0.21.0

langgraph==0.2.60
groq==0.13.1
